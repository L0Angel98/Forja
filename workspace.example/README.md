# workspace.example/

Workspace de agente de ejemplo (`soul.md`, `planta.md`, `memoria.md`, `rutinas/`, `conectores.yaml`). Se copia a `workspace/` en cada instalación y lo edita el rol admin.

> Contenido real agregado en la spec 12 (workspace y runtime).
